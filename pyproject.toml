[build-system]
requires = ["hatchling>=1.26"]
build-backend = "hatchling.build"

[project]
name = "sds200"
version = "0.24.0"
description = "Python control library for Uniden SDS100, SDS150, and SDS200 scanners"
readme = "README.md"
requires-python = ">=3.11"
license = "MIT"
license-files = ["LICENSE"]
authors = [
  { name = "Steven Boyd", email = "steven.boyd78@gmail.com" }
]
keywords = [
  "radio",
  "scanner",
  "sds100",
  "sds150",
  "sds200",
  "serial",
  "udp",
  "uniden",
]
classifiers = [
  "Development Status :: 3 - Alpha",
  "Environment :: Console",
  "Intended Audience :: Developers",
  "Programming Language :: Python",
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3.11",
  "Programming Language :: Python :: 3.12",
  "Programming Language :: Python :: 3.13",
  "Programming Language :: Python :: 3.14",
  "Topic :: Communications :: Ham Radio",
  "Topic :: Software Development :: Libraries :: Python Modules",
  "Typing :: Typed",
]
dependencies = [
  "argcomplete>=3.6,<4",
  "pyserial>=3.5",
  "rich>=13.9,<16",
]

[project.optional-dependencies]
playback = [
  "sounddevice>=0.5.3,<0.6",
]

tui = [
  "textual>=8.2,<9",
]

web = [
  "fastapi>=0.139,<1",
  "uvicorn>=0.51,<1",
]

mqtt = [
  "paho-mqtt>=2.1,<3",
]

dev = [
  "build>=1.2",
  "fastapi>=0.139,<1",
  "httpx2>=2.7,<3",
  "mypy>=1.10",
  "paho-mqtt>=2.1,<3",
  "pytest>=8.0",
  "pytest-cov>=5.0",
  "ruff>=0.6",
  "textual>=8.2,<9",
  "twine>=6.0",
  "types-pyserial>=3.5.0.20250326",
  "uvicorn>=0.51,<1",
]

[project.urls]
Homepage = "https://github.com/stevenboyd78/sdsctl"
Repository = "https://github.com/stevenboyd78/sdsctl"
Issues = "https://github.com/stevenboyd78/sdsctl/issues"
Changelog = "https://github.com/stevenboyd78/sdsctl/blob/main/CHANGELOG.md"

[project.scripts]
sdsctl = "sds200.cli:main"

[tool.hatch.build.targets.wheel]
packages = ["src/sds200"]

[tool.pytest.ini_options]
addopts = "-ra"
testpaths = ["tests"]

[tool.coverage.report]
fail_under = 86

[tool.ruff]
target-version = "py311"
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM"]

[tool.mypy]
python_version = "3.11"
strict = true
packages = ["sds200"]
mypy_path = "src"
exclude = ["^src/sds200/home_assistant_integration/"]
